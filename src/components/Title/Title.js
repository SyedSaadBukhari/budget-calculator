import React from "react";

import "./title.scss";

const Title = () => {
  return (
    <article className="app-title">
      <img src="/images/budgest-tracker-icon.png" alt="Budget Tracker Logo" />
      <h1 className="app-heading">Budget Tracker</h1>
    </article>
  );
};

export default Title;
